# Core data stack
pandas>=2.0.0
numpy>=1.24.0

# Machine learning
scikit-learn>=1.3.0
lightgbm>=4.0.0

# Financial data – Polygon.io (primary provider, requires POLYGON_API_KEY env var)
# Install the official client for the richest API support (recommended):
#   pip install polygon-api-client
# The PolygonProvider falls back to raw `requests` if this is not installed.
polygon-api-client>=1.12.0

# HTTP (used by PolygonProvider fallback when polygon-api-client is not installed)
requests>=2.28.0

# Financial data – yfinance (optional fallback; no API key required)
# Used when POLYGON_API_KEY is not set and provider="auto" or provider="yfinance".
yfinance>=0.2.36

# Evaluation & plotting
matplotlib>=3.7.0
seaborn>=0.12.0
scipy>=1.11.0

# Parquet support (required for saving/loading .parquet files)
pyarrow>=12.0.0

# Utilities
joblib>=1.3.0
tqdm>=4.65.0
